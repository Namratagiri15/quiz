import { VisitedDirective } from './visited.directive';

describe('VisitedDirective', () => {
  it('should create an instance', () => {
    const directive = new VisitedDirective();
    expect(directive).toBeTruthy();
  });
});
